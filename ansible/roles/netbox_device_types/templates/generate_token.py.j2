import pynetbox
import pprint
import yaml
 
nb_conn = pynetbox.api(url='https://localhost')
 
token = nb_conn.create_token('{{ netbox_username }}', '{{ netbox_admin_password }}')
 
pprint.pprint(dict(token))
 
token.delete()